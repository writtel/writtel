{
  "private": true,
  "workspaces": [
    "writtel-cli-utils",
    "writtel-cloud",
    "writtel-opulent-theme",
    "writtel-server",
    "writtel-wp-importer"
  ],
  "scripts": {
    "build": "npm-run-all build:writtel-cloud",
    "build:writtel-cloud": "cd writtel-cloud; yarn build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
